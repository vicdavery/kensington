version: 2
general:
  build_dir: build
jobs:
  build:
    machine:
      - image: circleci/classic:201708-01
    steps:
      - run: 
        - name: Install cmake
        - command: 'sudo apt-get install cmake'
      - checkout
      - run: 
        - name: Config CMake
        - command: 'cmake ..'
